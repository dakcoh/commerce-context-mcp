spring:
  main:
    banner-mode: off
    # STDIO 모드는 HTTP/SSE가 불필요하므로 웹 서버(Tomcat)를 띄우지 않는다.
    # 콜드 스타트 시간과 메모리를 줄이고 포트 점유도 없앤다. MCP 통신은 stdin/stdout으로만 한다.
    web-application-type: none
  ai:
    mcp:
      server:
        stdio: true

logging:
  config: classpath:logback-stdio.xml
